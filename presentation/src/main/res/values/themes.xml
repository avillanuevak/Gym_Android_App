<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Projecte" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Projecte" parent="Base.Theme.Projecte" />
    <style name="Widget.App.DropDownMenu" parent="Widget.MaterialComponents.TextView">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">14sp</item>
    </style>
    <!-- Add this to your themes.xml file -->
    <style name="DeleteDialogTheme" parent="ThemeOverlay.MaterialComponents.MaterialAlertDialog">
        <item name="android:windowBackground">@drawable/dialog_background_transparent</item>
        <item name="buttonBarPositiveButtonStyle">@style/AlertDialogButton.Delete</item>
        <item name="buttonBarNegativeButtonStyle">@style/AlertDialogButton.Cancel</item>
        <item name="materialAlertDialogTitleTextStyle">@style/DialogTitle</item>
        <item name="materialAlertDialogBodyTextStyle">@style/DialogMessage</item>
    </style>

    <style name="DialogTitle" parent="MaterialAlertDialog.MaterialComponents.Title.Text">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">18sp</item>
    </style>

    <style name="DialogMessage" parent="MaterialAlertDialog.MaterialComponents.Body.Text">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="AlertDialogButton.Delete" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:textColor">@color/red_500</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="AlertDialogButton.Cancel" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">14sp</item>
    </style>
</resources>